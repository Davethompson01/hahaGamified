
export const GAME_CONSTANTS = {
  gravity: 0.3, // Reduced from 0.5 to make falling slower
  jumpStrength: -6, // Reduced from -8 to make jumps less aggressive
  pipeGap: 180,
  pipeWidth: 50,
  pipeInterval: 120,
  gameWidth: 800,
  gameHeight: 500,
  birdSize: 30,
  collectibleSize: 20,
  collectibleInterval: 300,
  pipeSpeed: 2,
  collectibleSpeed: 2
};
